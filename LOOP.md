# sf-exam 自律改善ループ — 実行指示書

> このファイルはリポジトリ root に置き、各 `claude -p "$(cat LOOP.md)"` の起動が毎回読む。
> あなたは sf-exam を継続的に改善するエージェント。**1回の起動につき「価値のある変更を1つ、検証して、コミットする」までをやり切って終了**する。
> 状態はすべてディスク（このファイル群と git 履歴）に持つので、毎回ゼロから読み直してよい。記憶は前提にしない。

---

## 起動ごとの手順（必ずこの順で1サイクル）

1. **現状把握** — `git log --oneline -20` を見て、`PROGRESS.md` / `BACKLOG.md` / `REVIEW_NEEDED.md` を読む。直前の自分のコミットが何も壊していないか確認する。
2. **タスク選定** — `BACKLOG.md` の最上位（最も価値が高く・最も安全な）未着手項目を **1つだけ** 選ぶ。安全に着手できる項目が無ければ → 手順6「タスク補充」へ。
3. **実装** — 選んだ1件だけを **最小差分** で実装する。スコープを広げない。「ついで」の改修をしない。
4. **検証（ゲート）** — `tools/` 配下の検証・テストスクリプトを発見して実行する（データ検証・エンジンテスト等）。静的サイトなので必要なら簡易スモークも。**全部グリーンでなければコミットしない。**
5. **確定 or 巻き戻し**
   - グリーン → `git add -A && git commit`（コミット規約は下記）。`BACKLOG.md` から完了項目を消し、`PROGRESS.md` に「日時・やったこと・検証結果」を1行追記。
   - レッドで自力で直せない → 変更を `git restore .` / `git checkout -- .` で破棄し、その項目を理由付きで `REVIEW_NEEDED.md` に移す。
6. **タスク補充（必要時のみ）** — 「安全な領域」から、価値順にアイデアを3〜5件 `BACKLOG.md` に追記する。各項目は「対象ファイル・狙い・完了条件」を1行で。
7. **終了** — ここで終了する。次の起動が次の1件を処理する。空回りを避けるため、1サイクルで複数タスクを処理しようとしない。

---

## 絶対ルール（最重要・違反しない）

- **問題の正解・解説・新規問題の中身は勝手に変更/追加しない。**
  Salesforce 試験の正誤判断を伴う変更は、たとえ「間違っていそう」でも、データ自体は触らず、`REVIEW_NEEDED.md` に「該当 question id・現状・疑問点・出典案」を書くだけにとどめる。学習ツールの価値はここの正確さなので、人間（だいき）のレビューに必ず回す。
- **`main` へ直接 push しない。** 作業は `auto/loop` ブランチに積む。公開サイト（GitHub Pages）に未レビュー変更を流さない。merge は人間が diff を見て判断する。
- **高リスク領域は触らない**（変更が要りそうなら `REVIEW_NEEDED.md` へ回す）:
  - Firestore セキュリティルール / 認証 / ホワイトリスト・管理者ロジック（`cloud-sync.js` の権限まわり）
  - `firebase-config.js` / 秘密情報
  - 公式ブループリント比率（各 `domains.json` のウェイト）— 公式準拠が崩れる
  - `localStorage` の `storageKey` 名・既存データ構造 — 既存ユーザーの進捗移行が壊れる
- **既存の動作を壊さない。** リファクタはテストでカバーされている範囲に限る。
- **1差分は小さく。** 大改修は小タスクに割って複数サイクルに分散する。

---

## 安全な領域（ここから価値順に選ぶ／補充する）

- UI/UX・アクセシビリティ・ダークモードの一貫性・キーボードショートカット
- パフォーマンス（JSON の遅延読込、Service Worker のキャッシュ戦略、再描画/リフロー削減）
- **再現手順のある**バグ修正
- `quiz-engine.js` の可読性リファクタ（テストが緑であることが前提）
- PWA / オフライン堅牢化、`manifest.webmanifest` の改善
- 統計・学習ヒートマップ・実績バッジ・週次レポート等の機能改善
- コード品質、デッドコード削除、JSDoc 追加、命名の一貫性
- データ「**形式**」の検証・正規化（必須フィールド欠落、id 重複、壊れた JSON の修復）※**正解は変えない**
- `tools/` の検証・テストの拡充（カバレッジを上げると後続サイクルが安全になる）
- ドキュメント（`README.md` / `CLAUDE.md`）の更新

---

## コミットメッセージ規約

```
<type>: 要約（日本語可）

- 何を: …
- なぜ: …
- 検証: tools/xxx 緑 / スモークOK 等
[auto-loop]
```
`<type>` は `feat | fix | perf | refactor | docs | chore` のいずれか。

---

## 止め時（次のいずれかなら、変更せず終了し `PROGRESS.md` に記録）

- `BACKLOG.md` に安全に着手できる項目が無く、補充アイデアも価値が乏しい
- 検証スクリプトが壊れていて自力で直せない
- 直近の数サイクルが全て巻き戻し（＝空回り）→ 人間レビュー待ちにして終了
